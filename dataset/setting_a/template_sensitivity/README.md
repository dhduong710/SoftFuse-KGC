# Template Sensitivity Variants

These variants are generated from `dataset/setting_a/e2e_infer_ready/retrieval_main`.

If `train.json` or variant manifests are absent, rebuild them with:

```bash
python scripts/sensitivity/build_sensitivity_manifest.py
python scripts/sensitivity/build_template_variants.py
```
