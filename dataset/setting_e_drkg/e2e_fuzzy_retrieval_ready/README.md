# DRKG E2E Fuzzy-Retrieval Package

This package is generated from `dataset/setting_e_drkg/e2e_soft_support_ready/rgcn_sweep_selected`.

If `train.json` is absent under `rgcn/`, rebuild it with:

```bash
python scripts/drkg/build_fuzzy_retrieval.py
```
