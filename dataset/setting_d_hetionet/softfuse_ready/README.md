# Hetionet SoftFuse Ready Package

This package is generated from the Hetionet splits, graph, and selected R-GCN backbone source.

If `train.json` is absent in a lightweight checkout, regenerate it with:

```bash
python scripts/hetionet/build_softfuse_ready.py
```
