# Hetionet E2E Soft-Support Package

This package is generated from `dataset/setting_d_hetionet/softfuse_ready`.

If `train.json` is absent, rebuild it with:

```bash
python scripts/hetionet/build_soft_support.py
```
