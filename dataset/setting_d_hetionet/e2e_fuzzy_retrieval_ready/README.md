# Hetionet E2E Fuzzy-Retrieval Package

This package is generated from the Hetionet soft-support package.

If `train.json` is absent, rebuild it with:

```bash
python scripts/hetionet/build_fuzzy_retrieval.py
```
