# repoDB E2E Fuzzy-Retrieval Package

This package is generated from `dataset/setting_f_repodb/e2e_soft_support_ready/rgcn_raw_display_control`.

If `train.json` is absent under `rgcn/`, rebuild it with:

```bash
python scripts/repodb/build_fuzzy_retrieval.py
```
