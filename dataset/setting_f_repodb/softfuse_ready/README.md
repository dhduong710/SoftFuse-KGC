# repoDB SoftFuse Ready Packages

The source-specific folders here are generated from repoDB splits, graph, and structure baseline outputs.

If a source folder is missing `train.json`, regenerate the packages with:

```bash
python scripts/repodb/build_softfuse_ready.py
```
